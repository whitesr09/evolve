(()=>{const KEY='evolve_session_history_v1';const modes=[['#finishLab','Decision Lab','🧠'],['#finishSocial','Social Arena','🎭'],['#finishCareer','Career Sim','💼'],['#finishBusiness','Business Sim','📈'],['#finishEnglish','English Arena','🗣️']];const read=()=>{try{const v=JSON.parse(localStorage.getItem(KEY)||'[]');return Array.isArray(v)?v:[]}catch{return[]}};const write=v=>localStorage.setItem(KEY,JSON.stringify(v.slice(0,30)));function add(name,icon){const a=read(),now=new Date();a.unshift({name,icon,at:now.toISOString()});write(a)}function fmt(iso){const d=new Date(iso),today=new Date();const same=d.toDateString()===today.toDateString();return same?`Today · ${d.toLocaleTimeString([],{hour:'2-digit',minute:'2-digit'})}`:d.toLocaleDateString([],{month:'short',day:'numeric'})}function render(){const body=document.getElementById('journeyContent');if(!body)return;let host=document.getElementById('sessionHistory');if(!host){host=document.createElement('section');host.id='sessionHistory';host.className='journey-card glass sh-card';const controls=[...body.querySelectorAll('.journey-card')].find(x=>x.textContent.includes('Controls'));controls?body.insertBefore(host,controls):body.appendChild(host)}const a=read();host.innerHTML=`<div class="journey-section-title"><div><p class="eyebrow">RECENT ACTIVITY</p><h3>Session history</h3></div><span class="subtle">${a.length}</span></div>${a.length?`<div class="sh-list">${a.slice(0,8).map(x=>`<div class="sh-row"><span class="sh-icon">${x.icon}</span><div><b>${x.name}</b><small>${fmt(x.at)}</small></div><span class="sh-done">✓</span></div>`).join('')}</div>`:'<p class="subtle sh-empty">Complete a training session and it will appear here.</p>'}`}function schedule(){setTimeout(render,80)}document.addEventListener('click',e=>{for(const [sel,name,icon] of modes){if(e.target.closest(sel)){add(name,icon);break}}if(e.target.closest('#profileBtn'))schedule()},true);const css=document.createElement('style');css.textContent='.sh-card{margin-top:12px}.sh-list{display:grid;gap:8px;margin-top:10px}.sh-row{display:grid;grid-template-columns:38px 1fr auto;align-items:center;gap:10px;padding:9px 0;border-bottom:1px solid var(--line)}.sh-row:last-child{border-bottom:0}.sh-icon{width:36px;height:36px;border-radius:12px;display:grid;place-items:center;background:rgba(127,127,127,.08);border:1px solid var(--line)}.sh-row b{display:block;font-size:11px}.sh-row small{display:block;margin-top:3px;font-size:9px;color:var(--muted)}.sh-done{font-weight:900;opacity:.6}.sh-empty{font-size:10px;margin:10px 0 0}';document.head.appendChild(css)})();